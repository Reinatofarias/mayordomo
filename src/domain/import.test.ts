import {test} from 'node:test';import assert from 'node:assert/strict';import {parseCsv,mapImport} from './import.ts';
test('CSV handles quoted separators, escaped quotes, BOM and newlines',()=>{const rows=parseCsv('\uFEFFfecha,detalle,importe\r\n2026-09-01,"Compra, mercado",10.99\r\n2026-09-02,"Una ""compra""",12');assert.equal(rows[1][1],'Compra, mercado');assert.equal(rows[2][1],'Una "compra"');});
test('CSV rejects malformed input without partial import',()=>{assert.throws(()=>parseCsv('a,b\n"broken,2'));assert.throws(()=>parseCsv('a,b\n1,2,3'));});
test('mapping retains exact minor units and rejects invalid calendar dates',()=>{const mapping={description:1,date:0,amount:2,kind:'SIGNED' as const,categoryId:'food',currency:'USD'};assert.equal(mapImport([['2026-09-01','Compra','-10.99']],mapping)[0].amount_minor,'1099');assert.equal(mapImport([['2026-09-01','Ingreso','10.99']],mapping)[0].kind,'INCOME');assert.throws(()=>mapImport([['2026-02-30','Compra','1']],mapping));});
